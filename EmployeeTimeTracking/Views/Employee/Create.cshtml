@model EmployeeTimeTracking.Models.EmployeeRequestModel

@{
    ViewData["Title"] = "Add Employee";
}
<div class="container">
    <main role="main" class="pb-3">
        <h1>Add Employee</h1>

        <form asp-action="Create" method="post">
            <div class="form-group">
                <label asp-for="FirstName" class="control-label">Meno</label>
                <input asp-for="FirstName" class="form-control" />
                <span asp-validation-for="FirstName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="LastName" class="control-label">Preizvisko</label>
                <input asp-for="LastName" class="form-control" />
                <span asp-validation-for="LastName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Position" class="control-label">Pracovná pozícia</label>
                <input asp-for="Position" class="form-control" />
                <span asp-validation-for="Position" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="HireDate" class="control-label">Dátum nástupu</label>
                <input asp-for="HireDate" class="form-control" type="date" />
                <span asp-validation-for="HireDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Pridať zamestnanca" class="btn btn-primary" />
            </div>
        </form>
    </main>
</<div>
@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
